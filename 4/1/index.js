import { readInput } from '../../utils/readInput.js'

const Day4Pt1 = () => {
  const input = readInput(4, 1, true)
  const rows = Array.from(input.split("\r\n"))
  const result = "foobar"
  console.log("Day 4 part 1:")
  console.log(result)
}

export default Day4Pt1
