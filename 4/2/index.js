import { readInput } from '../../utils/readInput.js'

const Day4Pt2 = () => {
  const input = readInput(4, 2, true)
  const rows = Array.from(input.split("\r\n"))
  const result = "foobar"
  console.log("Day 4 part 2:")
  console.log(result)
}

export default Day4Pt2
