import { readInput } from '../../utils/readInput.js'

const Day2Pt1 = () => {
  const input = readInput(2, 1, true);
  const rows = Array.from(input.split("\r\n"));
  console.log(rows);

  const result = "foobar"
  console.log("Day 2 part 1:")
  console.log(result);
}

export default Day2Pt1
